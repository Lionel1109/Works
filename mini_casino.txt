#include <iostream>
#include <cstdlib>
using namespace std;

int main()
{
    setlocale(0, ""); 
    int a; 
    int b; 
    int c = 100; 
    int d; 

    cout << "Привет! Тебя приветствует рулетка! В начале игры у тебя имеется 100 монет." << endl;
    cout << "Твоя задача, заработать 1000 монет, делая ставки на цвета:" << endl;
    cout << "черный выиграет если выпадет: 20-68" << endl;
    cout << "красный выиграет если выпадет: 67-99" << endl;
    cout << "голубой выиграет если выпадет: 2-19" << endl;
    cout << "золотой выиграет если выпадет: 1 или 100" << endl;
    cout << "Черный - x2 монет(Шанс 48%), Красный - x3 монет(Шанс 32%), Голубой - x5монет(Шанс 18%) и золотой - x50 монет(Шанс 2%)" << endl;

    while (c < 1000)
    {
        if (c > 0) 
        {
            cout << endl << "Введите Вашу ставку: ";
            cin >> a;
        }
        else if (c <= 0) 
        {
            cout << "ты все профукал! На твоем счету " << c << " монет" << endl;
            system("pause");
            return 0;
        }
        if (c < a) 
        {
            while (c < a)
            {
                cout << "Проверь карманы, не хватает средств!" << endl;
                cout << "Уменьшите ставку: ";
                cin >> a;
            }
        }

        cout << "Выбери цвет ставки - (1 — Черный; 2 — Красный; 3 — Голубой; 4 — Золотой): ";
        cin >> b;

        while (b > 4) 
        {
            cout << "Цвета под таким номером нет, введите корректный номер - (1 — Черный; 2 — Красный; 3 — Голубой; 4 — Золотой): ";
            cin >> b;
        }
        while (b < 1)
        {
            cout << "Цвета под таким номером нет, введите корректный номер - (1 — Черный; 2 — Красный; 3 — Голубой; 4 — Золотой): ";
            cin >> b;
        }

        d = rand() % 100 + 1; 
        cout << "Выпал " << d << endl;

        if (b == 1) 
        {
            if (20 <= d && d <= 68) 
            {
                c = c + a; 
                cout << "Твоя ставка выиграла! У вас " << c << " монет" << endl;
            }
            else {
                c = c - a; 
                cout << "Ваша ставка проиграла, у вас " << c << " монет" << endl;
            }
        }
        else if (b == 2) 
        {
            if (67 <= d && d <= 99)
            {
                c = c + a * 2; 
                cout << "Твоя ставка выиграла! У вас " << c << " монет" << endl;
            }
            else {
                c = c - a;
                cout << "Ваша ставка проиграла:( У вас " << c << " монет" << endl;
            }
        }
        else if (b == 3) 
        {
            if (2 <= d && d <= 19)
            {
                c = c + a * 4;
                cout << "Отлично! Ставка выиграла, у вас " << c << " монет" << endl;
            }
            else {
                c = c - a;
                cout << "Ваша ставка проиграла, у вас " << c << " монет" << endl;
            }
        }
        else if (b == 4) 
        {
            if (d == 1)
            {
                c = c + a * 49;
                cout << "JACKPOT! Here comes the money$$$ У вас " << c << " монет" << endl;
            }
            else if (d == 100)
            {
                c = c + a * 49;
                cout << "JACKPOT! Here comes the money$$$ У вас " << c << " монет" << endl;
            }
            else {
                c = c - a;
                cout << "И на что ты надеялся? у тебя " << c << " монет" << endl;
            }
        }
    }

    if (c > 1000) 
    {
        cout << "Поздравляю! Вы победили в этой игре. На вашем счету " << c << " монет" << endl;
        system("pause");
        return 0;
    }
}